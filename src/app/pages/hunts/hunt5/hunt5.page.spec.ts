import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Hunt5Page } from './hunt5.page';

describe('Hunt5Page', () => {
  let component: Hunt5Page;
  let fixture: ComponentFixture<Hunt5Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Hunt5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
