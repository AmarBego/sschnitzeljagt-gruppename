import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Hunt3Page } from './hunt3.page';

describe('Hunt3Page', () => {
  let component: Hunt3Page;
  let fixture: ComponentFixture<Hunt3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Hunt3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
