import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Hunt6Page } from './hunt6.page';

describe('Hunt6Page', () => {
  let component: Hunt6Page;
  let fixture: ComponentFixture<Hunt6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Hunt6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
