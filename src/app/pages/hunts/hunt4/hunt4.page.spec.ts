import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Hunt4Page } from './hunt4.page';

describe('Hunt4Page', () => {
  let component: Hunt4Page;
  let fixture: ComponentFixture<Hunt4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Hunt4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
