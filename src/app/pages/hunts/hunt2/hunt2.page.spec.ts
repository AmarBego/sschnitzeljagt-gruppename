import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Hunt2Page } from './hunt2.page';

describe('Hunt2Page', () => {
  let component: Hunt2Page;
  let fixture: ComponentFixture<Hunt2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Hunt2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
